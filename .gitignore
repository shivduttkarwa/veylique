# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
node_modules/
shopify-upload/

# Shopify CLI files
.shopify/

## Release files
release
*.zip

# Old static-demo artifacts kept locally for reference only. These are NOT
# referenced by any section/block/snippet and are excluded from deploys.
assets/bootstrap*.css
assets/bootstrap*.js
assets/swiper*.css
assets/rainyday*.js
assets/inter*
assets/demo-vid.mp4
assets/footer-video.mp4

# IMPORTANT: gsap / ScrollTrigger / SplitText / swiper JS and the clash-display
# fonts ARE runtime dependencies of the theme (animations, sliders, headings)
# and MUST stay committed so the Shopify GitHub integration deploys them.
# Do NOT re-add them to .gitignore.
